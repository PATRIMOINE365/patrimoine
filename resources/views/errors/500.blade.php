{{--
    HTTP 500. The words, the code and the contact details all come from
    the error catalogue, so this page cannot drift from what the Error
    codes page says about PM-9904.
--}}

@include('errors.layout', ['code' => 'PM-9904'])
