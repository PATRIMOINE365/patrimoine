{{--
    Shared visual baseline for every generated PDF document (V1.1.0).

    Included AFTER each template's own <style> block, deliberately
    limited to brand alignment: headings and table chrome pick up the
    Patrimoine forest-green identity while template-specific layout
    stays untouched.
--}}
<style>
    h1 {
        color: #123527;
    }

    .document-title {
        color: #123527;
    }

    table th {
        background-color: #eef4f0 !important;
        color: #123527;
        border-color: #d7e2db !important;
    }

    table td {
        border-color: #d7e2db !important;
    }

    .section-title {
        color: #1d5c3f;
    }
</style>
